Check that every task this run claims to complete for `{{inputs.feature}}` maps to an actual requirement — a spec/plan/ticket line, or (absent one) the original feature request. Flag anything implemented that isn't asked for, and anything asked for that isn't implemented.

Report the result plainly, as the last thing you do: full coverage with no unrequested scope means say so explicitly ("spec-coverage: clean"); any gap, either direction, needs the specific mismatch (what's missing, what's extra). This step is not gated by a marker file — report accurately; the human at `open-mr-gate` is the run's real checkpoint for this.
