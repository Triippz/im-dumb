# Copy to a local gitignored file or export in the shell. Never commit real keys.
JUDGE_API_KEY=
JUDGE_MODEL=
JUDGE_MODEL_VERSION=
# Optional: when set, must differ from JUDGE_MODEL (self-preference guard).
GENERATOR_MODEL=
